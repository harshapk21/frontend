/**
 * label -> l small , htmlFor="id" , good practise
 * without defining value property , cant do doc.getById('').getAttribute('value) , but access directly like getById.value();
 * default value and normal value are not same , we also have min , max , required when in form etc
 * form => onSubmit is sttr
 * handleSubmit = (e)=>{
 * const formData = new FormData(e.target);
 * formData.get('name')
 * 
 * }
 * input , label , button  , a , span , img all are inline
 * 
 */

/**
 * className not class , no need of "" for css even for colors
 */